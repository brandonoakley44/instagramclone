import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        margin: 5,
        borderRadius: 40,     // half of width and height
        borderWidth: 3,
        borderColor: "#ae22e0"
    },
    image: {
        borderRadius: 40,     // half of width and height
        borderWidth: 1,
        borderColor: "#ae22e0"
    }
});

export default styles;